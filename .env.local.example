# Copy this file to .env.local for the Next.js app, and to workers/.env for the Python workers.
# Fill in real values; keep .env files out of git (already in .gitignore).

# ─── Supabase (project: autojob) ─────────────────────────────────────────────
NEXT_PUBLIC_SUPABASE_URL=https://bgxkqlavqnzaxpinlefg.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_Dm7JC7SLheAokf-dZ_pfvw_CD9O1hJK
# Server / worker only — never expose client-side. Get from Supabase → Settings → API → service_role
SUPABASE_SERVICE_ROLE_KEY=

# ─── Anthropic (same key works as Outreach) ──────────────────────────────────
ANTHROPIC_API_KEY=

# ─── Gmail (info@getaiappgenius.com — same App Password as Outreach) ─────────
GMAIL_USER=info@getaiappgenius.com
GMAIL_APP_PASSWORD=

# ─── Public app URL (used for tracking pixel + share links) ─────────────────
NEXT_PUBLIC_APP_URL=http://localhost:3000

# ─── Owner login (single-user dashboard auth) ───────────────────────────────
APP_PASSWORD=
